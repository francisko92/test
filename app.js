let members=[];const $=s=>document.querySelector(s);async function loadMembers(){const r=await fetch('./members.json',{cache:'no-store'});members=await r.json()}function normalize(v){return(v||'').trim().toUpperCase()}function render(id){const result=$('#result');const member=members.find(m=>normalize(m.id)===normalize(id));result.classList.add('show');if(!member){result.innerHTML=`<div class="notfound"><strong>Carnet no trobat.</strong><br>Comprova el codi o contacta amb l'Almenar Futsal.</div>`;return}const active=member.status==='ACTIU';result.innerHTML=`<div class="member"><div><span class="status ${active?'active':'inactive'}"><span class="dot"></span>${member.status}</span><h3>${member.display_name}</h3><div class="code">${member.id}</div><div class="meta"><div><label>Temporada</label><strong>${member.season}</strong></div><div><label>Alta</label><strong>${member.member_since||'—'}</strong></div></div></div><div class="shield">V</div></div>`}async function init(){await loadMembers();const params=new URLSearchParams(location.search);const id=params.get('id');if(id){$('#memberCode').value=id;render(id)}$('#verifyForm').addEventListener('submit',e=>{e.preventDefault();const v=$('#memberCode').value;history.replaceState({},'',`?id=${encodeURIComponent(v)}`);render(v)})}init();let deferredPrompt;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;const b=document.querySelector('#installBtn');if(b){b.style.display='inline-flex';b.onclick=async()=>{deferredPrompt.prompt();deferredPrompt=null;b.style.display='none'}}});if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js');